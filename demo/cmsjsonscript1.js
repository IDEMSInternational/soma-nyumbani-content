//Objective: to write a script that takes the cms html of any session and converts it into a json file in which the headings are automatically converted into json elements and internal html is preserved
function headonly(cms) {
    //will take a cms file, read it, 
}